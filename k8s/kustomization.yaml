apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: recipe

resources:
  - namespace.yaml
  - postgres.yaml
  - backend.yaml
  - frontend.yaml
  # ingress.yaml 은 선택 사항 — README 참고(프론트 재빌드 필요)
